#include<iostream>

using namespace std;

int main(){
        // types of datatype:

    // type of int:
    short a;
    int b;
    long c;
    long long d;

    // types of float
    float score = 45.34;
    double score2 = 45.332;
    long double score3 = 45.234533;
    score = 34.2;
    cout<<"The score is: "<<score<<endl;

    int const s = 31; //using const for not changing the value of the variable.
    // s = 34; it gives an error if you print;
}

